
{
  "lastUpdate": "Hari Ini",
  "message": "Market Sudah Buka!",
  "apps": [
    {
      "id": "com.elinaexperience.qapp",
      "name": "Elina experience",
      "description": "Ngobrol bareng Elina yuk!",
      "icon": "fa-comment",
      "color": "#ff69b4",
      "version": "1.0",
      "downloadUrl": "https://raw.githubusercontent.com/vimediacorporation01-prog/quizos-store/refs/heads/main/apps/elina_experience.qapp.json"
    }
 {
  "id": "com.grapichsspreadtest.qapp",
  "name": "Grapichs Spreadtest",
  "description": "Ayo uji kekuatan grafis!",
  "icon": "fa-chart-area",
  "color": "#ffff00",
  "version": "2.0",
  "html": "<!-- START OF FILE text/html -->\n<!DOCTYPE html>\n<html lang=\"id\">\n<head>\n\n    <meta charset=\"UTF-8\">\n    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">\n    <title>VGrApichs Benchmark: Idgitaf Edition</title>\n    <style>\n        @import url('https://fonts.googleapis.com/css2?family=Rajdhani:wght@500;700&display=swap');\n\n        body { margin: 0; overflow: hidden; background: #050505; font-family: 'Rajdhani', sans-serif; color: #fff; }\n\n        /* MENU SCREEN */\n        #menu {\n            position: fixed; top: 0; left: 0; width: 100%; height: 100%;\n            background: radial-gradient(circle at center, #1a1a1a, #000);\n            display: flex; flex-direction: column; align-items: center; justify-content: center;\n            z-index: 2000;\n        }\n        h1 { font-size: 3rem; margin-bottom: 5px; color: #00ffcc; text-shadow: 0 0 30px #00ffcc; letter-spacing: 3px; text-align: center; }\n        .subtitle { font-size: 1rem; color: #aaa; margin-bottom: 30px; letter-spacing: 2px; }\n        \n        .mode-btn {\n            width: 90%; max-width: 350px; padding: 15px; margin: 10px; background: rgba(0,0,0,0.5);\n            border: 2px solid #333; color: white; font-size: 1.2rem; font-weight: bold;\n            cursor: pointer; transition: 0.2s; position: relative; overflow: hidden;\n            font-family: 'Rajdhani', sans-serif;\n        }\n        .mode-btn:active { transform: scale(0.95); }\n\n        #btn-lite { border-color: #00ff00; color: #00ff00; }\n        #btn-std { border-color: #00ccff; color: #00ccff; }\n        #btn-vp { border-color: #ff0055; color: #ff0055; }\n\n        /* LOADING */\n        #loader {\n            position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: #000;\n            display: none; justify-content: center; align-items: center; z-index: 3000; flex-direction: column;\n        }\n        .spinner {\n            width: 40px; height: 40px; border: 4px solid #333; border-top: 4px solid #00ffcc;\n            border-radius: 50%; animation: spin 1s linear infinite; margin-bottom: 20px;\n        }\n        @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }\n\n        /* HUD */\n        #hud { position: fixed; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; display: none; }\n        .stat-box {\n            position: absolute; top: 20px; left: 20px;\n            font-size: 1.2rem; color: #fff; text-shadow: 1px 1px 0 #000;\n            background: rgba(0,0,0,0.5); padding: 10px; border-radius: 5px;\n        }\n        .progress-bar { position: absolute; top: 0; left: 0; height: 4px; background: #00ffcc; width: 0%; transition: width 0.1s linear; }\n\n        /* RESULTS */\n        #result-screen {\n            position: fixed; top: 0; left: 0; width: 100%; height: 100%;\n            background: #000; display: none; flex-direction: column;\n            align-items: center; justify-content: center; z-index: 4000;\n        }\n        .score-val { font-size: 5rem; font-weight: bold; color: #ffcc00; text-shadow: 0 0 30px #ffcc00; margin: 20px 0; }\n        .score-detail { font-size: 1rem; color: #ccc; text-align: center; line-height: 1.6; }\n        #btn-retry { margin-top: 30px; padding: 10px 40px; background: white; color: black; border: none; font-weight: bold; cursor: pointer; }\n\n    </style>\n    <script type=\"importmap\">\n        {\n            \"imports\": {\n                \"three\": \"https://unpkg.com/three@0.160.0/build/three.module.js\",\n                \"three/addons/\": \"https://unpkg.com/three@0.160.0/examples/jsm/\"\n            }\n        }\n    </script>\n</head>\n<body>\n\n    <!-- MENU -->\n    <div id=\"menu\">\n        <h1>VGrApichs</h1>\n        <div class=\"subtitle\">BENCHMARK: IDGITAF EDITION</div>\n        \n        <button id=\"btn-lite\" class=\"mode-btn\" onclick=\"startTest('LITE')\">LITE MODE<br><span style=\"font-size:0.8rem; opacity:0.7;\">HP KENTANG / MID</span></button>\n        <button id=\"btn-std\" class=\"mode-btn\" onclick=\"startTest('STD')\">STANDARD<br><span style=\"font-size:0.8rem; opacity:0.7;\">PC / LAPTOP</span></button>\n        <button id=\"btn-vp\" class=\"mode-btn\" onclick=\"startTest('VP')\">VP - EXTREME<br><span style=\"font-size:0.8rem; opacity:0.7;\">RTX HIGH-END</span></button>\n    </div>\n\n    <!-- LOADER -->\n    <div id=\"loader\">\n        <div class=\"spinner\"></div>\n        <div id=\"load-text\" style=\"color:white; letter-spacing:2px;\">MENGUNDUH LAGU...</div>\n    </div>\n\n    <!-- HUD -->\n    <div id=\"hud\">\n        <div class=\"progress-bar\" id=\"prog-bar\"></div>\n        <div class=\"stat-box\">\n            FPS: <span id=\"fps-counter\" style=\"color:#0f0\">0</span><br>\n            OBJ: <span id=\"obj-counter\">0</span><br>\n            <span id=\"shape-info\" style=\"font-size:0.8rem; color:#aaa;\"></span>\n        </div>\n    </div>\n\n    <!-- RESULTS -->\n    <div id=\"result-screen\">\n        <div style=\"font-size: 1.5rem; color: #aaa;\">VGscore</div>\n        <div class=\"score-val\" id=\"final-score\">0</div>\n        <div class=\"score-detail\" id=\"score-details\"></div>\n        <button id=\"btn-retry\" onclick=\"location.reload()\">RESTART</button>\n    </div>\n\n    <script type=\"module\">\n        import * as THREE from 'three';\n        import { EffectComposer } from 'three/addons/postprocessing/EffectComposer.js';\n        import { RenderPass } from 'three/addons/postprocessing/RenderPass.js';\n        import { UnrealBloomPass } from 'three/addons/postprocessing/UnrealBloomPass.js';\n        import { AfterimagePass } from 'three/addons/postprocessing/AfterimagePass.js';\n\n        // --- LINK LAGU HARDCODED ---\n        const MUSIC_URL = \"https://raw.githubusercontent.com/vimediacorporation01-prog/VGrApichs-/f5c49d0ed0bd7ec864acb945ae199f8ff0973b7a/Idgitaf%20-%20Sedia%20Aku%20Sebelum%20Hujan%20(Official%20Lyric%20Video)(Copy).mp3\";\n        // ---------------------------\n\n        let mode = 'LITE';\n        let isRunning = false;\n        let scene, camera, renderer, composer;\n        let objects = [];\n        let audio;\n        \n        let stats = { frames: 0, totalFps: 0, frameCount: 0, maxObjects: 0, startTime: 0, fpsHistory: [] };\n        let lastTime = performance.now();\n\n        // Mode Configurations\n        const MODES = {\n            'LITE': { spawnRate: 3, maxObjCap: 5000, postProcess: false, scoreMulti: 1 },\n            'STD': { spawnRate: 5, maxObjCap: 10000, postProcess: true, scoreMulti: 5 },\n            'VP': { spawnRate: 30, maxObjCap: 60000, postProcess: true, scoreMulti: 20 }\n        };\n\n        // Assets\n        const geoPool = {\n            lite: [\n                new THREE.BoxGeometry(2,2,2),\n                new THREE.ConeGeometry(1.5, 3, 16),\n                new THREE.CylinderGeometry(1, 1, 3, 16),\n                new THREE.CapsuleGeometry(1, 2, 4, 8),\n                new THREE.TorusGeometry(1.5, 0.5, 12, 32)\n            ],\n            std: [new THREE.TorusKnotGeometry(1, 0.3, 64, 8), new THREE.IcosahedronGeometry(2, 1)],\n            vp: [new THREE.TorusKnotGeometry(1, 0.3, 100, 16), new THREE.SphereGeometry(1, 32, 32)]\n        };\n\n        const matPool = {\n            lite: new THREE.MeshStandardMaterial({ color: 0x00ff00, roughness: 0.5, metalness: 0.1, flatShading: false }),\n            std: new THREE.MeshStandardMaterial({ color: 0x00ccff, roughness: 0.2, metalness: 0.8 }),\n            vp: new THREE.MeshStandardMaterial({ color: 0xff0055, roughness: 0.1, metalness: 1.0, emissive: 0x550000, emissiveIntensity: 2 })\n        };\n\n        // --- START LOGIC ---\n        window.startTest = (m) => {\n            mode = m;\n            document.getElementById('menu').style.display = 'none';\n            document.getElementById('loader').style.display = 'flex';\n\n            audio = new Audio(MUSIC_URL);\n            audio.crossOrigin = \"anonymous\"; // PENTING UNTUK GITHUB\n            audio.volume = 0.6;\n            \n            // Event Listeners\n            audio.addEventListener('canplaythrough', startEngine);\n            audio.addEventListener('error', (e) => {\n                alert(\"Gagal memuat lagu dari GitHub. Pastikan Repo Public atau koneksi lancar.\");\n                document.getElementById('loader').style.display = 'none';\n                document.getElementById('menu').style.display = 'flex';\n            });\n            audio.addEventListener('ended', finishTest);\n        };\n\n        function startEngine() {\n            if(isRunning) return;\n            isRunning = true;\n            \n            document.getElementById('loader').style.display = 'none';\n            document.getElementById('hud').style.display = 'block';\n\n            initThree();\n            audio.play();\n            stats.startTime = performance.now();\n            animate();\n        }\n\n        function initThree() {\n            scene = new THREE.Scene();\n            scene.background = new THREE.Color(0x050505);\n            scene.fog = new THREE.FogExp2(0x000000, mode==='VP' ? 0.002 : 0.005);\n\n            camera = new THREE.PerspectiveCamera(70, window.innerWidth/window.innerHeight, 0.1, 1000);\n            camera.position.z = 50;\n\n            renderer = new THREE.WebGLRenderer({ antialias: mode !== 'VP', powerPreference: \"high-performance\" });\n            renderer.setSize(window.innerWidth, window.innerHeight);\n            renderer.shadowMap.enabled = (mode !== 'LITE'); \n            document.body.appendChild(renderer.domElement);\n\n            // Lighting\n            const dir = new THREE.DirectionalLight(0xffffff, 1.5);\n            dir.position.set(10, 20, 10);\n            if(mode !== 'LITE') {\n                dir.castShadow = true;\n                dir.shadow.mapSize.set(1024, 1024);\n            }\n            scene.add(dir);\n            scene.add(new THREE.AmbientLight(0xffffff, 0.5));\n\n            // Post Process\n            if(MODES[mode].postProcess) {\n                composer = new EffectComposer(renderer);\n                composer.addPass(new RenderPass(scene, camera));\n                const bloom = new UnrealBloomPass(new THREE.Vector2(window.innerWidth, window.innerHeight), 1.5, 0.4, 0.85);\n                bloom.threshold = 0.1; bloom.strength = mode==='VP'?2.5:1.2; bloom.radius = 0.5;\n                composer.addPass(bloom);\n\n                if(mode === 'VP') {\n                    const blur = new AfterimagePass();\n                    blur.uniforms[\"damp\"].value = 0.8;\n                    composer.addPass(blur);\n                }\n            }\n        }\n\n        function animate() {\n            if(!isRunning) return;\n            requestAnimationFrame(animate);\n\n            const now = performance.now();\n            const delta = now - lastTime;\n            lastTime = now;\n            const fps = 1000/delta;\n            \n            stats.totalFps += fps; stats.frameCount++; stats.fpsHistory.push(fps);\n            if(stats.frameCount % 15 === 0) document.getElementById('fps-counter').innerText = Math.round(fps);\n            document.getElementById('obj-counter').innerText = objects.length;\n\n            if(audio.duration) {\n                const pct = audio.currentTime / audio.duration;\n                document.getElementById('prog-bar').style.width = (pct*100) + '%';\n                \n                // Spawn Rate increases with song\n                const multiplier = 1 + (pct * 5); \n                const count = Math.floor(MODES[mode].spawnRate * multiplier);\n                \n                if(objects.length < MODES[mode].maxObjCap) spawn(count);\n            }\n\n            // Animation\n            if(mode === 'VP') {\n                camera.position.x += (Math.random()-0.5) * 0.5;\n                camera.position.y += (Math.random()-0.5) * 0.5;\n                // Brutal random reset (VRAM killer)\n                for(let i=0; i<objects.length; i+=20) {\n                    if(Math.random()>0.9) objects[i].position.set((Math.random()-0.5)*100, (Math.random()-0.5)*100, (Math.random()-0.5)*100);\n                }\n            } else {\n                camera.rotation.z = Math.sin(now * 0.0005) * 0.2;\n            }\n\n            objects.forEach(o => {\n                o.rotation.x += 0.02; o.rotation.y += 0.02;\n                if(mode !== 'VP') {\n                    o.position.z += 0.2;\n                    if(o.position.z > 50) o.position.z = -150;\n                }\n            });\n\n            if(composer) composer.render();\n            else renderer.render(scene, camera);\n        }\n\n        function spawn(n) {\n            let geo, mat;\n            \n            if(mode === 'LITE') {\n                // LOGIKA GANTI BENTUK (Setiap 10 Detik)\n                const phaseDuration = 10; \n                const phase = Math.floor(audio.currentTime / phaseDuration) % geoPool.lite.length;\n                geo = geoPool.lite[phase];\n                \n                const shapeNames = [\"KUBUS\", \"KERUCUT\", \"SILINDER\", \"KAPSUL\", \"DONAT\"];\n                document.getElementById('shape-info').innerText = \"FASE: \" + shapeNames[phase];\n                \n                const colors = [0x00ff00, 0xffaa00, 0x00ccff, 0xff00ff, 0xffff00];\n                mat = matPool.lite.clone();\n                mat.color.setHex(colors[phase]);\n\n            } else if (mode === 'STD') {\n                geo = geoPool.std[Math.floor(Math.random()*geoPool.std.length)];\n                mat = matPool.std;\n            } else {\n                geo = geoPool.vp[Math.floor(Math.random()*geoPool.vp.length)];\n                mat = matPool.vp.clone();\n                mat.color.setHex(Math.random()*0xffffff);\n            }\n\n            for(let i=0; i<n; i++) {\n                const mesh = new THREE.Mesh(geo, mat);\n                const range = mode==='VP' ? 100 : 80;\n                mesh.position.set((Math.random()-0.5)*range*2, (Math.random()-0.5)*range*2, -Math.random()*200 - 50);\n                mesh.rotation.set(Math.random()*3, Math.random()*3, 0);\n                if(mode!=='LITE') mesh.scale.setScalar(Math.random()*2+0.5);\n                scene.add(mesh); objects.push(mesh);\n            }\n            if(objects.length > stats.maxObjects) stats.maxObjects = objects.length;\n        }\n\n        function finishTest() {\n            isRunning = false;\n            document.getElementById('hud').style.display = 'none';\n            document.getElementById('result-screen').style.display = 'flex';\n            \n            const avgFps = stats.totalFps / stats.frameCount;\n            const variance = stats.fpsHistory.reduce((a,b)=>a+Math.abs(b-avgFps),0) / stats.fpsHistory.length;\n            const stability = Math.max(0, 100 - (variance*2));\n            const duration = (performance.now() - stats.startTime)/1000;\n            \n            let score = (avgFps * 50) + (stats.maxObjects / 15);\n            score *= MODES[mode].scoreMulti;\n            if(duration < 10) score *= 0.1;\n\n            const finalScore = Math.floor(score);\n            \n            let d = 0; const step = Math.ceil(finalScore/100);\n            const tm = setInterval(()=>{\n                d+=step; \n                if(d>=finalScore){ d=finalScore; clearInterval(tm); }\n                document.getElementById('final-score').innerText = d.toLocaleString();\n            },10);\n\n            document.getElementById('score-details').innerHTML = `\n                MODE: <span style=\"color:#fff\">${mode}</span><br>\n                AVG FPS: ${avgFps.toFixed(1)}<br>\n                PEAK GEOMETRY: ${stats.maxObjects.toLocaleString()}<br>\n                STABILITY: ${stability.toFixed(1)}%\n            `;\n        }\n\n        window.addEventListener('resize', () => {\n            camera.aspect = window.innerWidth / window.innerHeight;\n            camera.updateProjectionMatrix();\n            renderer.setSize(window.innerWidth, window.innerHeight);\n            if(composer) composer.setSize(window.innerWidth, window.innerHeight);\n        });\n    </script>\n</body>\n</html>",
  "script": ""
}
  ]
}
